[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "luma-forge-provisioner-worker"
version = "1.0.0"
description = "Container-side Provisioner Worker for preparing LumaForge ComfyUI workspaces"
requires-python = ">=3.12"
dependencies = ["huggingface_hub>=1,<2"]

[project.optional-dependencies]
dev = []

[tool.luma_forge_provisioner]
http_port = 8000
default_workspace_mount_path = "/workspace"

[tool.setuptools.packages.find]
where = ["src"]
